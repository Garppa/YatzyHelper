/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package Pelimoottori;

import backend.Noppa;
import backend.Tulosruudut.Tyyppi;
import java.util.ArrayList;
import java.util.List;

/**
 *
 * @author intoit
 */
public class noppakasittelya {
 /*   
for(Tyyppi tyyppi : alustus.annaTyypit()){
            System.out.println(tyyppi.toString());
        }
        
        alustus.nopat.heitaNopat();
        
        System.out.println(alustus.nopat.toString());
        int valinta = 10;
        List<Noppa> nopat = new ArrayList<Noppa>();
        nopat.addAll(alustus.nopat.annaNopat());
        
        while(true){
            
            for(Noppa noppa : nopat) {
                System.out.println(noppa.annaSilmaluku());
            }
            
            System.out.println("valitse nopat");
            while (true) {
                valinta = Integer.parseInt(lukija.nextLine());
                if (valinta == 0 ) {
                    break;
                }
                alustus.nopat.valitseNoppa(nopat.get((valinta-1)));
            }
            alustus.nopat.heitaNopat();
            nopat.clear();
            nopat.addAll(alustus.nopat.annaNopat());
        }
    */
}
